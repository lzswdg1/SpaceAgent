# Optional Services

`multi-agent-orchestrator` is the TypeScript/LangGraph.js compute-only Multi-Agent
reasoning boundary. Java `apps/platform-server` remains the business, Runtime, Provider,
Tool and persistence authority.

The retired Python AI orchestrator and Java runtimes remain available only through Git
history; they are not supported deployment paths.
