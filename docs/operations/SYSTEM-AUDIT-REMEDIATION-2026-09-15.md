# 全系统审计整改交付记录

状态：源码整改与确定性验证完成（不等于生产/外部验收）。私有源码标识和本地报告路径不随公开快照分发。

## 已实施

- S01/S02：记忆项目/任务祖先必须匹配当前租户，所有写端点检查组织写权限。
- S03：Identity 当前执行资格独立于 Run 配置快照，模型每次新dispatch、Tool、Project/Governance和旧URL刷新
  重新检查当前权限；个人旧 Knowledge Embedding 检查当前用户状态。失效不会消耗审批或伪装新的已知效果。
- D01/D02：Artifact对象锁协调引用/保留/删除；删除准入后新增保留明确冲突，旧错误数据在领取时阻断。
  每staging有独立发布/保留/访问/存储身份，同staging重试稳定，不跨用户或待删对象去重；配额如实计量。
- S04/F03：普通文件工具禁止Git元数据，私有bundle传输保留；新Workspace detached HEAD，不创建执行分支；
  管理员物理清理涵盖无remoteUrl的受管理空白源，普通逻辑删除仍保留代码。
- F01/F02：已知支持的模型发送dimensions；百炼v3/v4使用最多10条批次，超限在请求前拒绝。
- P01/P02：公共HTTP单次总deadline30秒、URL链累计45秒；Worker按有限前缀读取UTF8，不先全量载入。
- P03/P05：稳定Markdown分块、变量高度历史窗口、共享memo消息组件；串行轮询、信号取消和全部终态停止。
- P04/P06：100条Agent/Source分页、无Prompt选择器摘要、用户显式加载更多和精确选择补查；RAG两次批量回源、
  按base复核ACL，不逐候选重复查权限，保留最终活动代次/hash与撤权校验。
- M01/M02：关键Artifact生命周期展开为可阅读代码，抽取Actor准入、Chat输出、分页/轮询/消息/Markdown组件；
  当前架构与浏览器存储/AgentVersion旧规范明确纠正。没有搬迁业务权威或无关大重写历史类。
- 管理后端新增可信资源观察API/类型化内部wire/独立鉴权/审计，管理员前端源码不变；口径见ADMIN-BUSINESS-EVIDENCE。

## 数据与部署

V1096仅去掉跨发布内容唯一约束，保留既有内容/引用/地址；新存储key加入staging身份，不被旧删除任务共享。
V1097扩展Runtime观测类型与同execution去重索引。已应用迁移不修改；当前运行平台数据不迁移，不重启任何服务。
源代码版本门禁1097/109。上线需先备份并协调迁移/激活业务、Admin后端、Worker和租户Web；不承诺旧进程混部支持。

## 验证与未运行

已通过：新增权限/删除/资源观察集中测试；真实隔离PG mutex/旧版本升级/指标范围快照；Web147、Admin Web34，
两端构建；Worker27；浏览器113（3种语言/尺寸），气泡、手动阅读、一次跳转不跟随、侧栏及布局截图检查；架构/diff。
最终顺序完整Maven：884项，878通过、6项可选Tika/Milvus环境依赖跳过，失败/错误0；package、架构/diff和受影响
Compose quiet config通过。追加纯SQL回源/观察PG2/2通过，不依赖Milvus/Tika。所有夹具是临时数据，非真实用户动作。
首次完整回归存在旧迁移断言与并行编译产物冲突；修正并改为顺序执行后的上述完整gate通过，没有放宽生产保护。
原始日志为本机临时证据、不随仓库分发；持久证据为本页摘要。

外部验收仍未执行：生产Linux/runsc、OS硬磁盘配额/IO隔离、真实Provider非默认维度/大批量、RAG质量/HA、实际
账单对账、生产TLS/告警/容量与在线依赖漏洞情报。资源采样不是硬配额，不能把它宣传为所有资源实耗的完整计量。
公网不可信代码继续默认禁用；密码-only管理员是保留的产品选择，不擅自恢复MFA。
